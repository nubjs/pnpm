---
"pacquet": patch
---

A host embedding the engine can name the Node.js executable that lifecycle and package scripts see as `NODE` and `npm_node_execpath`. Standalone pnpm still records the first `node` on `PATH`.
