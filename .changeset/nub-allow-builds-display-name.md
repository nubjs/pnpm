---
"pacquet": patch
---

The `ignored-builds` hint, the `add --allow-build` rejection and the notice an interactive `approve-builds` prints when it denies every build now name the build allow-list and the program through the embedder profile. A host that keeps its approvals under a name of its own no longer points its users at `allowBuilds` and `pnpm rebuild`.
