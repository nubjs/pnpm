---
"pacquet": patch
---

A host embedding the engine can name the executable that provides the package manager a git-hosted dependency pins, such as `packageManager: yarn@1.22.22`, while the dependency is prepared. Standalone pnpm still provides it through the running `pnpm`.
