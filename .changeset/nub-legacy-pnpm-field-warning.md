---
"pacquet": patch
---

The warning about migrated keys under `package.json`'s `pnpm` field names the running program, and leaves out the pointer to pnpm's settings page under an embedder that reads none of pnpm's configuration.
